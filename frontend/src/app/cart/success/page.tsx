import { CheckoutSuccess } from '@/components/CartPage/CheckoutSuccess'

export const metadata = {
  title: 'Order Confirmed | Royale Relax',
  description: 'Your Royale Relax order has been confirmed.',
}

export default function CartSuccessPage() {
  return <CheckoutSuccess />
}
