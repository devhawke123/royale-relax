import { LoginPageClient } from '@/components/LoginPage/LoginPageClient'

export const metadata = {
  title: 'Sign In | Royale Relax',
  description: 'Sign in or create an account with Royale Relax.',
}

export default function LoginPage() {
  return <LoginPageClient />
}
